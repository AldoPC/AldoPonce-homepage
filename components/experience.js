import {Box} from '@chakra-ui/react'
import styled from '@emotion/styled'

export const ExperienceSection = styled(Box)`
padding-left: 3.4em;
text-indent: -3.4em;
`
export const ExperienceBold = styled.span`
font-weight: bold;
`